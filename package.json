{
  "name": "streetbuzz",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "npm run build --prefix web && npm run build --prefix studio && node scripts/assemble-studio.mjs"
  }
}
